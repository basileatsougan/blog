{
  "buildCommand": "cd frontend && npm ci && ng build",
  "outputDirectory": "frontend/dist/blog/browser",
  "installCommand": "cd frontend && npm ci",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}